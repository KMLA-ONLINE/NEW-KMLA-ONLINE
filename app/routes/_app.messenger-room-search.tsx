export const handle = {
  showMobileHeader: false,
  showMobileTabBar: false,
}

// Search UI is rendered by the messenger shell so result selection can return
// to the room pane and focus the matched message consistently.

export default function MessengerRoomSearchRoute() {
  return null
}
