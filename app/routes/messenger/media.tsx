export const handle = {
  showMobileHeader: false,
  showMobileTabBar: false,
}

// TODO(backend): add a loader here that fetches only this room's media
// (images/files) on demand, instead of deriving it from already-loaded
// messages in SharedMediaPane.

export default function MessengerRoomMediaRoute() {
  return null
}
