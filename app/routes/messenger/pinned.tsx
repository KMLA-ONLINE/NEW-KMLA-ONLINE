export const handle = {
  showMobileHeader: false,
  showMobileTabBar: false,
}

// Pinned-messages UI is rendered by the messenger shell so desktop can keep
// it in the secondary pane and mobile can replace the room/details panel
// consistently.

export default function MessengerRoomPinnedRoute() {
  return null
}
