export const handle = {
  showMobileHeader: false,
  showMobileTabBar: false,
}

// Members UI is rendered by the messenger shell so desktop can keep it in the
// secondary pane and mobile can replace the room/details panel consistently.

export default function MessengerRoomMembersRoute() {
  return null
}
